<% title = 'Editor de Blocos' %>

<section class="max-w-4xl mx-auto bg-gray-900 rounded-xl shadow-lg p-10 fade-in">
  <h1 class="text-3xl font-bold text-white mb-6 text-center">Editor de Blocos</h1>

  <div id="editor" class="space-y-4">
    <!-- Blocos dinâmicos -->
  </div>

  <div class="mt-6 text-center">
    <button onclick="addBlock()" class="px-6 py-2 bg-blue-600 hover:bg-blue-700 rounded-lg text-white font-semibold transition-all duration-300">
      Adicionar Bloco
    </button>
  </div>
</section>

<script>
  function addBlock() {
    const editor = document.getElementById('editor');
    const block = document.createElement('div');
    block.contentEditable = true;
    block.className = `
      w-full min-h-[60px] px-4 py-3 bg-gray-800 rounded-lg text-white 
      outline-none focus:ring-2 focus:ring-blue-500 placeholder-gray-500 transition
    `;
    block.setAttribute('data-placeholder', 'Digite algo aqui...');
    editor.appendChild(block);
    block.focus();
  }
</script>
